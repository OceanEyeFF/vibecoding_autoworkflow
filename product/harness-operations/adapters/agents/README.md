# Agents Adapters

`product/harness-operations/adapters/agents/` 保存 `Harness Operations` 在 Codex / OpenAI 侧的 repo adapter 源码。

本层以 `header.yaml` + backend metadata（如 `agents/openai.yaml`）为主，不直接维护最终 `SKILL.md`。
