# Simple Workflow Bindings

本对象默认不要求 repo-specific harness bindings。

## Optional

- `${GATE_SEQUENCE}`：如果宿主仓库希望显式命名 gate 顺序，可在输出要求中注入。

## Runtime Placeholders

- `${TASK_SOURCE_REF}`：当前任务来源文档或文件路径。
