# Manifests

本目录预留给后续的全局安装、打包和市场分发元数据。

当前阶段：

- 业务源码仍在 `product/memory-side/skills/` 与 `product/memory-side/adapters/`
- repo-local 测试挂载点仍在 `.claude/`、`.agents/` 与 `.opencode/`
- 部署行为由 `toolchain/scripts/deploy/adapter_deploy.py` 统一处理
