# Harness Manifests

`product/harness/manifests/` 预留给后续的 Harness 打包、安装和分发元数据。

当前阶段：

- 本目录只建立骨架，不回填历史 manifest
- `product/harness/skills/` 与 `product/harness/adapters/` 先作为后续实现层承接位
- repo-local deploy/runtime state 继续留在 `.autoworkflow/`、`.agents/`、`.claude/` 与 `.opencode/`
