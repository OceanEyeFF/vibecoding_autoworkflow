version: 1
defaults:
  backend: codex
  judge_backend: codex
  with_eval: true
runs:
  - repo: trackers
    task: context-routing
  - repo: yolov5
    task: context-routing
  - repo: TaxHacker
    task: context-routing
  - repo: tabula
    task: context-routing
