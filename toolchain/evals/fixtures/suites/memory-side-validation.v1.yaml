version: 1
defaults:
  backend: codex
  judge_backend: codex
  with_eval: true
runs:
  - repo: axum
    task: context-routing
  - repo: httpx
    task: context-routing
  - repo: vite-plus
    task: context-routing
  - repo: bors-ng
    task: context-routing
