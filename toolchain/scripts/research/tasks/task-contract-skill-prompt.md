Use the repo-local `task-contract-skill` in `.claude/skills` for this repository.

Produce the fixed-format `Task Contract` now in a single response.

Constraints:
- Do not ask follow-up questions.
- Use only confirmed facts from this prompt and repository context.
- Put unresolved items under `pending` or `Open Decisions` instead of guessing.
- Stop before implementation planning, agent assignment, or execution steps.

I have a task I want to work on, but it's still a bit unclear and not fully structured.

Here is what I know so far:
Improve debug tools to make them easier to use and more efficient, both for humans and AI.

I want to clarify:
- what exactly needs to be done
- what constraints or boundaries exist
- what a good outcome would look like

Please output only the `Task Contract`.
