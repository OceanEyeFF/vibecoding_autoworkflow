# Ideas

`docs/ideas/` 只保存尚未进入主线的想法，不保存当前项目真相。

目录规则：

- `active/`：frontmatter `status: active`
- `incubating/`：frontmatter `status: incubating`
- `archived/`：frontmatter `status: archived`

如果一个 idea 已经进入主线知识层，就不再留在 `ideas/` 里做双份维护。
