# Workflow Families

`docs/harness/workflow-families/` 固定可复用的 Harness workflow family policy。

当前入口：

- [repo-evolution/README.md](./repo-evolution/README.md)

workflow family 固定在文档真相层。当前 active policy 以 repo evolution append request routing 为主；superseded historical references 不作为当前主线 owner。若后续在 `product/harness/` 中出现对应 executable source，它以下游实现层身份消费这里的定义。
