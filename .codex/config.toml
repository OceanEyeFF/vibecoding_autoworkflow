# Repo-local Codex execution config.
# Keep this file small and explicit; long-term truth still belongs in docs/.

approval_policy = "on-request"
sandbox_mode = "workspace-write"
personality = "pragmatic"
model_reasoning_effort = "high"
web_search = "cached"

[features]
multi_agent = true
